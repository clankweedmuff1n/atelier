import { BgsetDirectiveDirective } from './bgset-directive.directive';

describe('BgsetDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BgsetDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
